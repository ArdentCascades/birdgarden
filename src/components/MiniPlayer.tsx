/**
 * MiniPlayer.tsx — Preact island: sticky bottom "now playing" bar
 *
 * Appears when audio is playing and the user scrolls away from the main player.
 * Features: track thumbnail, bird name, song type, play/pause, close button.
 *
 * Fully implemented in Task 9.
 */

// Stub — implementation in Task 9
export default function MiniPlayer() {
  return null; // Hidden by default
}
