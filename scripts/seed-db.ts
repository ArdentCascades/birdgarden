/**
 * seed-db.ts — Reads JSON seed data → inserts into SQLite
 *
 * Usage:
 *   bun run scripts/seed-db.ts
 *   bun run scripts/seed-db.ts --pack europe-v1  (future: region packs)
 *
 * Sets SEED_MODE=true so db.ts opens the connection in write mode.
 *
 * Implemented in Task 2.
 */

// Stub — implementation in Task 2
console.log('seed-db.ts — implemented in Task 2');
